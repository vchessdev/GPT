# 📚 devDA Blog System - Tài Liệu Hệ Thống Hoàn Chỉnh

**Phiên bản**: 1.0  
**Tên miền**: devda.undo.it/blog  
**Công nghệ**: PHP + JSON + HTML + CSS + JavaScript (Vanilla)  
**Ngày tạo**: 2026-02-24

---

## 📋 MỤC LỤC

1. [Sơ đồ kiến trúc hệ thống](#sơ-đồ-kiến-trúc-hệ-thống)
2. [Sơ đồ luồng dữ liệu](#sơ-đồ-luồng-dữ-liệu)
3. [Mô hình phân quyền](#mô-hình-phân-quyền)
4. [Cấu trúc JSON Database](#cấu-trúc-json-database)
5. [API Structure](#api-structure)
6. [Auth Flow](#auth-flow)
7. [Vote Flow](#vote-flow)
8. [Comment Flow](#comment-flow)
9. [Admin Flow](#admin-flow)
10. [Cấu trúc thư mục](#cấu-trúc-thư-mục)
11. [Code mẫu](#code-mẫu)
12. [Hướng dẫn cài đặt](#hướng-dẫn-cài-đặt)

---

## 🏗️ Sơ đồ kiến trúc hệ thống

```
┌─────────────────────────────────────────────────────────────┐
│                    CLIENT (Frontend)                        │
│  HTML + CSS + JavaScript (Vanilla) + Ajax                   │
└──────────────────────┬──────────────────────────────────────┘
                       │ HTTP/AJAX
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                    WEB SERVER (PHP)                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Frontend Pages (PHP)                   │   │
│  │ index.php | post.php | search.php | upload.php ... │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           API Layer (RESTful JSON)                  │   │
│  │ /api/auth.php  | /api/posts.php | /api/comments.php│   │
│  │ /api/votes.php | /api/files.php                    │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Admin Panel (PHP)                      │   │
│  │ /admin/index.php | /admin/dashboard.php            │   │
│  │ /admin/users.php | /admin/posts.php                │   │
│  └─────────────────────────────────────────────────────┘   │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ File I/O
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                  JSON Database Layer                        │
│  /data/users.json | /data/posts.json                        │
│  /data/comments.json | /data/votes.json                     │
│  /data/files.json | /data/logs.json                         │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ File System
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                    File Storage                             │
│  /uploads/images/ | /uploads/pdf/ | /uploads/docs/         │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 Sơ đồ luồng dữ liệu

### 1. Luồng Đăng nhập (Authentication Flow)
```
┌─────────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│   Browser   │────▶│ login.php│────▶│ auth.php │────▶│users.json│
│   (HTML)    │◀────│          │◀────│ (hash)   │◀────│          │
└─────────────┘     └──────────┘     └──────────┘     └──────────┘
    ▼ Session + Cookie
┌─────────────────────┐
│ Xác thực & Quyền hạn│
└─────────────────────┘
```

### 2. Luồng Tạo bài viết (Create Post Flow)
```
┌──────────────┐     ┌──────────┐     ┌─────────────┐     ┌────────────┐
│   Frontend   │────▶│ post.php │────▶│api/posts.php│────▶│ posts.json │
│ (HTML Form) │     │          │     │ (validate)  │     │            │
└──────────────┘     └──────────┘     └─────────────┘     └────────────┘
     │ Upload Image
     ▼
  ┌──────────────────┐     ┌──────────────┐
  │  upload.php      │────▶│ /uploads/    │
  │ (process file)   │     │  images/     │
  └──────────────────┘     └──────────────┘
```

### 3. Luồng Bình luận (Comment Flow)
```
Post Page ────▶ Comment Form ────▶ api/comments.php ────▶ comments.json
   ▲                ▲                      │
   │                │                      ▼
   └────────────────┴──────────────────── Refresh
```

### 4. Luồng Vote/Like (Vote Flow)
```
┌──────────┐     ┌────────────┐     ┌──────────────┐     ┌──────────┐
│   Like   │────▶│ vote.php   │────▶│api/votes.php │────▶│votes.json│
│  Button  │     │ (AJAX)     │     │ (check vote) │     │          │
└──────────┘     └────────────┘     └──────────────┘     └──────────┘
     │ Update count
     ▼
   Post Counter
```

---

## 👑 Mô hình phân quyền

```
┌─────────────────────────────────────────────────────┐
│             Role-Based Access Control (RBAC)        │
├─────────────────────────────────────────────────────┤
│                                                     │
│ 🔐 ADMIN                                            │
│  ├─ Xem dashboard thống kê                         │
│  ├─ Quản lý tất cả bài viết (edit/delete)         │
│  ├─ Quản lý tất cả bình luận (delete/hide)        │
│  ├─ Quản lý user (ban/promote)                    │
│  ├─ Quản lý file (delete/access)                  │
│  ├─ Xem logs hệ thống                             │
│  └─ Xem thống kê (views, likes, downloads)        │
│                                                     │
│ 👤 USER                                             │
│  ├─ Tạo bài viết (draft/publish)                  │
│  ├─ Edit/delete bài viết của mình                 │
│  ├─ Bình luận bài viết                            │
│  ├─ Delete bình luận của mình                     │
│  ├─ Vote/Like bài viết                            │
│  ├─ Upload file (giới hạn dung lượng)            │
│  ├─ Xem profile & posts của mình                  │
│  └─ Follow/Save posts (optional)                  │
│                                                     │
│ 🌐 GUEST                                            │
│  ├─ Xem danh sách bài viết (published)            │
│  ├─ Xem chi tiết bài viết                         │
│  ├─ Xem bình luận                                  │
│  ├─ Tìm kiếm bài viết                             │
│  └─ Tải file (nếu public)                         │
│                                                     │
└─────────────────────────────────────────────────────┘

Permissions Matrix:
┌────────────────┬────────┬────────┬────────┐
│ Action         │ Guest  │ User   │ Admin  │
├────────────────┼────────┼────────┼────────┤
│ View Published │ ✅    │ ✅    │ ✅    │
│ Create Post    │ ❌    │ ✅    │ ✅    │
│ Edit Own Post  │ ❌    │ ✅    │ ✅    │
│ Edit Any Post  │ ❌    │ ❌    │ ✅    │
│ Delete Any Post│ ❌    │ ❌    │ ✅    │
│ Comment        │ ❌    │ ✅    │ ✅    │
│ Like           │ ❌    │ ✅    │ ✅    │
│ Upload File    │ ❌    │ ✅    │ ✅    │
│ Manage Users   │ ❌    │ ❌    │ ✅    │
│ View Logs      │ ❌    │ ❌    │ ✅    │
└────────────────┴────────┴────────┴────────┘
```

---

## 📁 Cấu trúc JSON Database

### 1. users.json
```json
{
  "users": [
    {
      "id": "user_001",
      "username": "nguyenvan_a",
      "email": "van@example.com",
      "password": "$2y$10$...", // bcrypt hash
      "full_name": "Nguyễn Văn A",
      "avatar": "/uploads/images/avatar_001.jpg",
      "role": "user", // user | admin
      "status": "active", // active | banned | pending
      "bio": "Học sinh lớp 9",
      "created_at": "2026-02-01 10:30:00",
      "updated_at": "2026-02-24 10:30:00",
      "last_login": "2026-02-24 10:30:00",
      "email_verified": true
    }
  ]
}
```

### 2. posts.json
```json
{
  "posts": [
    {
      "id": "post_001",
      "author_id": "user_001",
      "title": "Cách học Toán hiệu quả",
      "slug": "cach-hoc-toan-hieu-qua",
      "content": "<h2>Nội dung bài viết...</h2>",
      "excerpt": "Chia sẻ kinh nghiệm học Toán...",
      "featured_image": "/uploads/images/post_001.jpg",
      "category": "Học Tập", // or array of categories
      "tags": ["toán", "học tập", "lớp 9"],
      "status": "published", // draft | published | archived
      "views": 150,
      "likes": 25,
      "dislikes": 2,
      "created_at": "2026-02-20 14:20:00",
      "updated_at": "2026-02-24 10:00:00",
      "published_at": "2026-02-20 14:20:00"
    }
  ]
}
```

### 3. comments.json
```json
{
  "comments": [
    {
      "id": "comment_001",
      "post_id": "post_001",
      "user_id": "user_002",
      "content": "Bài viết rất hữu ích!",
      "parent_id": null, // null = comment chính, "comment_001" = reply
      "status": "approved", // approved | pending | hidden
      "votes": 5,
      "created_at": "2026-02-21 10:30:00",
      "updated_at": "2026-02-21 10:30:00"
    }
  ]
}
```

### 4. votes.json
```json
{
  "votes": [
    {
      "id": "vote_001",
      "user_id": "user_001",
      "post_id": "post_001",
      "vote_type": "like", // like | dislike
      "created_at": "2026-02-21 10:30:00"
    }
  ]
}
```

### 5. files.json
```json
{
  "files": [
    {
      "id": "file_001",
      "uploader_id": "user_001",
      "filename": "Đề thi Toán lớp 9.pdf",
      "original_name": "De_thi_Toan_lop_9.pdf",
      "filepath": "/uploads/pdf/De_thi_Toan_lop_9_001.pdf",
      "file_type": "pdf", // pdf | image | doc
      "file_size": 2048576, // bytes
      "mime_type": "application/pdf",
      "post_id": "post_001", // nếu là file của bài viết
      "category": "Đề thi", // Đề thi | Tài liệu | Hình ảnh
      "access_level": "public", // public | private | members_only
      "downloads": 45,
      "created_at": "2026-02-20 14:20:00"
    }
  ]
}
```

### 6. logs.json
```json
{
  "logs": [
    {
      "id": "log_001",
      "user_id": "user_001",
      "action": "create_post", // create_post | edit_post | delete_post | login | logout
      "resource_id": "post_001",
      "resource_type": "post",
      "description": "Tạo bài viết mới",
      "ip_address": "192.168.1.1",
      "user_agent": "Mozilla/5.0...",
      "status": "success", // success | error
      "timestamp": "2026-02-20 14:20:00"
    }
  ]
}
```

---

## 🔌 API Structure

### Base URL
```
https://devda.undo.it/blog/api/
```

### 1. Authentication API (`/api/auth.php`)

**POST /api/auth.php?action=register**
```json
Request:
{
  "action": "register",
  "username": "nguyenvan_a",
  "email": "van@example.com",
  "password": "password123",
  "full_name": "Nguyễn Văn A"
}

Response (Success):
{
  "status": "success",
  "message": "Đăng ký thành công",
  "user_id": "user_001",
  "redirect": "/blog/login.php"
}

Response (Error):
{
  "status": "error",
  "message": "Email đã tồn tại"
}
```

**POST /api/auth.php?action=login**
```json
Request:
{
  "action": "login",
  "email": "van@example.com",
  "password": "password123",
  "remember": true
}

Response (Success):
{
  "status": "success",
  "message": "Đăng nhập thành công",
  "user": {
    "id": "user_001",
    "username": "nguyenvan_a",
    "email": "van@example.com",
    "role": "user"
  }
}
```

**POST /api/auth.php?action=logout**
```json
Response:
{
  "status": "success",
  "message": "Đã đăng xuất"
}
```

### 2. Posts API (`/api/posts.php`)

**POST /api/posts.php?action=create**
```json
Request:
{
  "action": "create",
  "title": "Tiêu đề bài viết",
  "content": "Nội dung bài viết",
  "category": "Học Tập",
  "tags": ["toán", "học tập"],
  "status": "draft"
}

Response:
{
  "status": "success",
  "message": "Tạo bài viết thành công",
  "post_id": "post_001",
  "slug": "tieu-de-bai-viet"
}
```

**GET /api/posts.php?action=list**
```json
Response:
{
  "status": "success",
  "data": [
    {
      "id": "post_001",
      "title": "Tiêu đề",
      "slug": "tieu-de",
      "excerpt": "Tóm tắt...",
      "author": "Nguyễn Văn A",
      "category": "Học Tập",
      "views": 150,
      "likes": 25,
      "comments_count": 5,
      "created_at": "2026-02-20 14:20:00"
    }
  ],
  "total": 50,
  "page": 1,
  "per_page": 10
}
```

### 3. Comments API (`/api/comments.php`)

**POST /api/comments.php?action=create**
```json
Request:
{
  "action": "create",
  "post_id": "post_001",
  "content": "Bình luận của tôi",
  "parent_id": null
}

Response:
{
  "status": "success",
  "comment_id": "comment_001"
}
```

### 4. Votes API (`/api/votes.php`)

**POST /api/votes.php?action=vote**
```json
Request:
{
  "action": "vote",
  "post_id": "post_001",
  "vote_type": "like"
}

Response:
{
  "status": "success",
  "total_likes": 26,
  "total_dislikes": 2,
  "user_vote": "like"
}
```

---

## 🔐 Auth Flow

```
┌──────────────────────────────────────────────────────────┐
│                    AUTHENTICATION FLOW                   │
└──────────────────────────────────────────────────────────┘

1. REGISTER (Đăng ký)
   ┌─────────────────┐
   │ User fills form │ (username, email, password)
   └────────┬────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Validate input       │ Check: empty, email format, length
   │ & Security check     │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Check exists?        │ user with email already?
   └────────┬─────────────┘
            │ ❌ Exists
            ├──────────────▶ Error: "Email đã tồn tại"
            │
            │ ✅ Not exists
            ▼
   ┌──────────────────────┐
   │ Hash password        │ password_hash() / bcrypt
   │ Generate user_id     │ uniqid() or UUID
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Save to users.json   │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Success response     │ Redirect to login
   └──────────────────────┘

2. LOGIN (Đăng nhập)
   ┌──────────────────────┐
   │ User fills form      │ (email, password)
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Validate input       │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Find user by email   │ Search users.json
   └────────┬─────────────┘
            │
            ├─ ❌ Not found ──▶ Error: "Email không tồn tại"
            │
            │ ✅ Found
            ▼
   ┌──────────────────────┐
   │ Verify password      │ password_verify()
   └────────┬─────────────┘
            │
            ├─ ❌ Invalid ──▶ Error: "Mật khẩu sai"
            │
            │ ✅ Valid
            ▼
   ┌──────────────────────┐
   │ Create session       │ $_SESSION['user_id']
   │ Set cookie (opt.)    │ setcookie('user_id', ..., time()+30days)
   │ Update last_login    │ In users.json
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Log action           │ Add to logs.json
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Success response     │ Redirect to home
   └──────────────────────┘

3. CHECK AUTH (Kiểm tra quyền)
   ┌──────────────────────┐
   │ Request → PHP        │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Check $_SESSION?     │
   └────────┬─────────────┘
            │
            ├─ Session exists ──▶ Get user_id
            │
            └─ No session ──▶ Check $_COOKIE
                              │
                              ├─ Cookie exists ──▶ Restore session
                              │
                              └─ No cookie ──▶ User = null (guest)

4. LOGOUT (Đăng xuất)
   ┌──────────────────────┐
   │ User clicks logout   │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Destroy session      │ session_destroy()
   │ Clear cookie         │ setcookie(..., time()-1)
   │ Log action           │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Redirect to home     │
   └──────────────────────┘
```

---

## 💬 Comment Flow

```
┌──────────────────────────────────────────────────────────┐
│                    COMMENT FLOW                          │
└──────────────────────────────────────────────────────────┘

1. VIEW COMMENTS
   ┌──────────────────┐
   │ Load post.php    │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Ajax: get posts  │
   │ with comments    │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────────────┐
   │ /api/comments.php?action │
   │ =list&post_id=post_001   │
   └────────┬─────────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Load comments.json   │ Filter by post_id
   │ Group by parent_id   │ Organize replies
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Return JSON array    │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Render in HTML       │ JavaScript template
   └──────────────────────┘

2. CREATE COMMENT
   ┌──────────────────┐
   │ Check auth?      │ Only logged-in users
   └────────┬─────────┘
            │
            ├─ ❌ Not logged in ──▶ Show login popup
            │
            │ ✅ Logged in
            ▼
   ┌──────────────────┐
   │ Show comment form│
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ User submits     │ POST form (AJAX)
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────────────┐
   │ /api/comments.php?action │
   │ =create                  │
   └────────┬─────────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Validate:           │ - Content not empty
   │ - user logged in    │ - Content length < 5000
   │ - post exists       │ - Post published
   │ - content length    │
   └────────┬─────────────┘
            │
            ├─ ❌ Invalid ──▶ Error response
            │
            │ ✅ Valid
            ▼
   ┌──────────────────────┐
   │ Sanitize HTML       │ strip_tags() / htmlspecialchars()
   │ Generate ID         │ uniqid()
   │ Save to comments.json│
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Log action          │
   │ Increment comment   │
   │ count in posts.json │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Return success       │
   │ New comment ID       │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Refresh comments     │ AJAX reload
   │ Show new comment     │
   └──────────────────────┘

3. DELETE COMMENT
   ┌──────────────────┐
   │ Admin or author? │
   └────────┬─────────┘
            │
            ├─ ❌ No permission ──▶ Error
            │
            │ ✅ Yes
            ▼
   ┌──────────────────────────┐
   │ /api/comments.php?action │
   │ =delete&id=comment_001   │
   └────────┬─────────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Delete from JSON     │ Remove entry
   │ Log action          │
   │ Update post count   │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Success response     │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Refresh comments     │
   └──────────────────────┘
```

---

## ⭐ Vote Flow

```
┌──────────────────────────────────────────────────────────┐
│                    VOTE FLOW (Like/Dislike)              │
└──────────────────────────────────────────────────────────┘

1. LOAD VOTE STATUS
   ┌──────────────────┐
   │ Load post page   │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Check user voted?    │
   │ (if logged in)       │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────────┐
   │ /api/votes.php?action=   │
   │ check&post_id=post_001   │
   └────────┬─────────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Search votes.json:   │
   │ user_id + post_id    │
   └────────┬─────────────┘
            │
            ├─ ❌ No vote found ──▶ Buttons active
            │
            │ ✅ Vote found
            ▼
   ┌──────────────────────┐
   │ Highlight button     │ Show user's previous vote
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Display vote counts  │ From posts.json
   └──────────────────────┘

2. SUBMIT VOTE
   ┌──────────────────┐
   │ User clicks      │ Like or Dislike button
   │ Like/Dislike     │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Check auth?      │ Only logged-in users
   └────────┬─────────┘
            │
            ├─ ❌ Not logged in ──▶ Show login popup
            │
            │ ✅ Logged in
            ▼
   ┌──────────────────────────┐
   │ AJAX POST to:            │
   │ /api/votes.php?action=   │
   │ vote                     │
   └────────┬─────────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Request body:        │
   │ {                    │
   │   post_id,           │
   │   vote_type: "like"  │
   │ }                    │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Check existing vote  │ Already voted?
   └────────┬─────────────┘
            │
            ├─ ✅ Yes (same vote)
            │  └──▶ Remove vote (toggle)
            │
            ├─ ✅ Yes (different vote)
            │  └──▶ Update vote
            │
            │ ❌ No
            ▼
   ┌──────────────────────┐
   │ Add new vote entry   │ Generate ID
   │ Save to votes.json   │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Update post counts   │ Increment/decrement
   │ in posts.json        │ likes/dislikes
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Log action           │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Return response:     │
   │ {                    │
   │   total_likes,       │
   │   total_dislikes,    │
   │   user_vote          │
   │ }                    │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Update UI:           │
   │ - Update counts      │
   │ - Highlight button   │
   └──────────────────────┘

Vote State Machine:
┌──────────────────────────────────┐
│ Initial State (No Vote)          │
│ Like button: Normal              │
│ Dislike button: Normal           │
└──────────────────┬───────────────┘
                   │ Click Like
                   ▼
        ┌──────────────────────────────────┐
        │ Like State                       │
        │ Like button: Highlighted (red)   │
        │ Dislike button: Normal           │
        └──────────────┬───────────────────┘
                       │ Click Like again (toggle)
                       ├──────────────────────────▶ No Vote State
                       │
                       │ Click Dislike
                       ▼
            ┌──────────────────────────────────┐
            │ Dislike State                    │
            │ Like button: Normal              │
            │ Dislike button: Highlighted (blue)│
            └──────────────────────────────────┘
```

---

## 👨‍💼 Admin Flow

```
┌──────────────────────────────────────────────────────────┐
│                    ADMIN FLOW                            │
└──────────────────────────────────────────────────────────┘

1. ADMIN LOGIN
   ┌──────────────────────┐
   │ /admin/login.php     │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ User enters email    │ admin_login_check.php
   │ and password         │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Check role = admin   │ In users.json
   └────────┬─────────────┘
            │
            ├─ ❌ Not admin ──▶ Error: "Bạn không có quyền"
            │
            │ ✅ Is admin
            ▼
   ┌──────────────────────┐
   │ Verify password      │
   │ Create session       │
   │ Log access           │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Redirect to          │
   │ /admin/dashboard.php │
   └──────────────────────┘

2. ADMIN DASHBOARD
   ┌──────────────────────┐
   │ /admin/dashboard.php │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────────┐
   │ Load all JSON files      │
   │ Calculate statistics:    │
   │ - Total users           │
   │ - Total posts           │
   │ - Total comments        │
   │ - Total views           │
   │ - Total likes           │
   │ - Recent activities     │
   └────────┬─────────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Display widgets:     │
   │ - User count         │
   │ - Post count         │
   │ - Comment count      │
   │ - View statistics    │
   │ - Recent logs        │
   └──────────────────────┘

3. MANAGE USERS
   ┌──────────────────────┐
   │ /admin/users.php     │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Load users.json      │
   │ Display table:       │
   │ - Username           │
   │ - Email              │
   │ - Role               │
   │ - Status             │
   │ - Created date       │
   │ - Actions (edit/ban) │
   └────────┬─────────────┘
            │
            ├─ Ban User
            │  │
            │  └──▶ Update status = "banned"
            │       Log action
            │
            ├─ Promote to Admin
            │  │
            │  └──▶ Update role = "admin"
            │       Log action
            │
            └─ Delete User
               │
               └──▶ Remove user entry
                    Remove related posts
                    Remove related comments
                    Log action

4. MANAGE POSTS
   ┌──────────────────────┐
   │ /admin/posts.php     │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Load posts.json      │
   │ Display table:       │
   │ - Title              │
   │ - Author             │
   │ - Status             │
   │ - Views              │
   │ - Comments           │
   │ - Created date       │
   └────────┬─────────────┘
            │
            ├─ Delete Post
            │  │
            │  └──▶ Remove post
            │       Remove related comments
            │       Delete uploaded files
            │       Log action
            │
            ├─ Edit Post
            │  │
            │  └──▶ Update title/content/status
            │       Log changes
            │
            └─ Archive Post
               │
               └──▶ Set status = "archived"
                    Hide from public

5. MANAGE COMMENTS
   ┌──────────────────────┐
   │ /admin/comments.php  │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Load comments.json   │
   │ Display table:       │
   │ - Content            │
   │ - Author             │
   │ - Post               │
   │ - Status             │
   │ - Date               │
   └────────┬─────────────┘
            │
            ├─ Hide Comment
            │  │
            │  └──▶ Set status = "hidden"
            │       Appears only to author & admin
            │
            ├─ Approve Comment
            │  │
            │  └──▶ Set status = "approved"
            │
            └─ Delete Comment
               │
               └──▶ Remove comment
                    Update post comment count
                    Log action

6. MANAGE FILES
   ┌──────────────────────┐
   │ /admin/files.php     │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Load files.json      │
   │ Display table:       │
   │ - Filename           │
   │ - Uploader           │
   │ - Size               │
   │ - Downloads          │
   │ - Access Level       │
   │ - Date               │
   └────────┬─────────────┘
            │
            ├─ Delete File
            │  │
            │  └──▶ Delete from files.json
            │       Delete from /uploads/
            │       Log action
            │
            └─ Change Access Level
               │
               └──▶ public | members_only | private
                    Log change

7. VIEW LOGS
   ┌──────────────────────┐
   │ /admin/logs.php      │
   └────────┬─────────────┘
            │
            ▼
   ┌──────────────────────┐
   │ Load logs.json       │
   │ Display table:       │
   │ - Action             │
   │ - User               │
   │ - Resource           │
   │ - IP Address         │
   │ - Status             │
   │ - Timestamp          │
   └────────┬─────────────┘
            │
            └──▶ Filter by user/action/date
                 Export logs (optional)
```

---

## 📂 Cấu trúc thư mục

```
/devda-blog-system/
│
├── index.php                    # Trang chủ
├── post.php                     # Chi tiết bài viết
├── login.php                    # Form đăng nhập
├── register.php                 # Form đăng ký
├── profile.php                  # Hồ sơ cá nhân
├── search.php                   # Tìm kiếm
│
├── /assets/
│   ├── /css/
│   │   ├── style.css            # CSS chính
│   │   ├── responsive.css       # Responsive design
│   │   └── admin.css            # CSS admin
│   │
│   ├── /js/
│   │   ├── main.js              # JavaScript chính
│   │   ├── api.js               # API request helper
│   │   ├── auth.js              # Auth logic
│   │   ├── vote.js              # Vote logic
│   │   ├── comment.js           # Comment logic
│   │   └── admin.js             # Admin logic
│   │
│   └── /images/
│       ├── logo.png
│       ├── favicon.ico
│       └── default-avatar.jpg
│
├── /data/
│   ├── users.json               # User data
│   ├── posts.json               # Post data
│   ├── comments.json            # Comment data
│   ├── votes.json               # Vote data
│   ├── files.json               # File data
│   └── logs.json                # Activity logs
│
├── /api/
│   ├── auth.php                 # Auth API (register, login, logout)
│   ├── posts.php                # Posts API (CRUD)
│   ├── comments.php             # Comments API (CRUD)
│   ├── votes.php                # Votes API
│   ├── files.php                # Files API
│   └── helpers.php              # Helper functions
│
├── /admin/
│   ├── index.php                # Admin home (redirect to dashboard)
│   ├── login.php                # Admin login form
│   ├── dashboard.php            # Admin dashboard
│   ├── users.php                # Manage users
│   ├── posts.php                # Manage posts
│   ├── comments.php             # Manage comments
│   ├── files.php                # Manage files
│   ├── votes.php                # View votes
│   ├── logs.php                 # View logs
│   └── /assets/
│       ├── /css/
│       └── /js/
│
├── /uploads/
│   ├── /images/                 # Post images
│   ├── /pdf/                    # PDF files
│   ├── /docs/                   # Document files
│   └── .gitkeep
│
├── /config/
│   ├── config.php               # Configuration file
│   └── database.php             # Database helper functions
│
├── README.md                     # Documentation
└── .htaccess                     # Apache config (optional)
```

---

## 🔧 Cấu trúc file quan trọng

### `/config/config.php` - Cấu hình hệ thống
```php
<?php
// Database & Data path
define('DATA_DIR', __DIR__ . '/../data/');
define('UPLOADS_DIR', __DIR__ . '/../uploads/');
define('ADMIN_EMAIL', 'admin@devda.undo.it');

// Security
define('SESSION_LIFETIME', 2592000); // 30 days
define('PASSWORD_HASH_ALGO', PASSWORD_BCRYPT);
define('PASSWORD_COST', 10);

// Pagination
define('POSTS_PER_PAGE', 10);
define('COMMENTS_PER_PAGE', 20);

// File upload
define('MAX_FILE_SIZE', 10485760); // 10MB
define('ALLOWED_IMAGE_TYPES', ['jpg', 'jpeg', 'png', 'gif']);
define('ALLOWED_DOC_TYPES', ['pdf', 'doc', 'docx', 'txt']);
?>
```

### `/config/database.php` - Hàm JSON database
```php
<?php
// Read JSON file
function readJSON($file) {
    $path = DATA_DIR . $file . '.json';
    if (!file_exists($path)) return null;
    return json_decode(file_get_contents($path), true) ?? [];
}

// Write JSON file
function writeJSON($file, $data) {
    $path = DATA_DIR . $file . '.json';
    return file_put_contents($path, json_encode($data, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE));
}

// Merge and save
function mergeJSON($file, $key, $newData) {
    $data = readJSON($file) ?? [];
    $data[$key] = $newData;
    return writeJSON($file, $data);
}
?>
```

---

## 💻 CODE MẪU

[Xem phần tiếp theo - Code Mẫu Chi Tiết]

---

**Tiếp theo**: Các file code mẫu sẽ được cung cấp trong phần 2 của tài liệu.
