# JWT Secret for token signing (REQUIRED - generate a strong random value)
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-key-generate-a-strong-random-value-here

# Optional: Server port (default: 3000)
PORT=3000

# Optional: Node environment
NODE_ENV=development
